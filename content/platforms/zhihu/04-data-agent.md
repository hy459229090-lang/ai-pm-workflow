---
tags:
  - platform/zhihu
  - series/04
---

# 让 AI 当你的数据分析师 — 数据取数自动化实践

> 本文是我用 AI 解决数据问题一年多的实践沉淀。不是教程，是一个真实工作流从 0 到 1 的演化过程。

---

## 一、为什么是数据取数？

过去这一年多，我一直在尝试用 AI 解决数据问题。毛球 App 上线 MVP 后，DAU 到了 12 万，老板开始频繁问各种数据：

"昨天的 DAU 怎么样？""社区发布率涨了吗？""附近服务的转化率多少？"

一开始我每次都是手动写 SQL 去查——`t_event_log` 里捞登录数据，`t_booking` 里看预约转化。后来发现同样的问题会被问第二遍、第三遍，每次都要重新想表结构和字段。

试过各种方案——从最早的"帮我看下这个表有什么数据"，到后来让 AI 直接生成 SQL、跑分析、出报表。

大部分尝试都失败了——要么 AI 理解不了业务口径，要么生成的东西没法直接用。

但有一个 Skill，我坚持用了一年多，而且用得越来越多：**数据取数自动化。**

一开始我只是想"省点事"——不用每次都跟 AI 描述毛球的表结构（`t_pet` 是宠物档案、`t_event_log` 是行为日志、`user_id < 1000` 是测试数据）。

但用了一年多后，我发现它带来的价值远超预期：

- **验证数据**：毛球首页从时间线改成推荐 feed 后，上线前看转化漏斗和功能渗透率
- **收集数据**：周报月报要的核心指标（DAU、留存、发布率），老板突然问的"宠物档案完善率多少"
- **产品分析**：用户行为路径（浏览帖子 → 点赞 → 关注）、功能使用情况、异常波动定位
- **快速汇报**：临时被叫去开会，5 分钟拉出毛球社区和商城的关键数据

它不再是一个"取数工具"，而是一个**全职数据分析师**。

这个过程我实践了一年多，效果很好——AI 对毛球表结构的理解比很多新入职的同事还快。

---

## 二、核心架构：数据知识库 + SQL 规则引擎

我的 `/data` Skill 不是简单的"SQL 生成器"。

它是一套完整的数据知识体系 + 分析能力：

```
用户问："毛球过去 7 天的 DAU 怎么样？"

↓

AI 内部执行：
1. 识别指标：DAU = 日活跃用户数
2. 查数据字典：用户行为日志在 t_event_log，user_id 标识用户
3. 应用业务规则：排除测试账号 (user_id < 1000)、只算正式环境
4. 生成 SQL → 执行 → 格式化输出
5. 可选：生成趋势图、对比上周、计算环比
```

**关键不是 AI 会写 SQL——是它理解毛球的业务数据。**

---

## 三、数据字典：AI 分析师的大脑

### 3.1 我的数据字典长这样

```markdown
## 核心业务表

### t_user（用户表）
| 字段 | 含义 | 示例 | 说明 |
|------|------|------|------|
| user_id | 用户 ID | 1001 | 从 1000 开始，<1000 是测试数据 |
| nickname | 昵称 | maoqiu_owner | |
| city | 城市 | 上海 | 用于附近服务排序 |
| created_at | 注册时间 | 2025-01-15 | |
| last_login_time | 最后登录时间 | 2025-03-20 | 判断活跃用这个 |

### t_pet（宠物档案）
| 字段 | 含义 | 示例 | 说明 |
|------|------|------|------|
| pet_id | 宠物 ID | 5001 | |
| user_id | 主人 ID | 1001 | 关联 t_user |
| species | 物种 | cat/dog | 用于推荐分类 |
| breed | 品种 | 英短/金毛 | |
| birth_date | 生日 | 2023-06-01 | 计算年龄 |
| weight_kg | 体重(kg) | 4.5 | 健康监控用 |

### t_post（内容表）
| 字段 | 含义 | 示例 | 说明 |
|------|------|------|------|
| post_id | 帖子 ID | 20001 | |
| user_id | 发布者 ID | 1001 | 关联 t_user |
| content_type | 内容类型 | image/video/text | |
| like_count | 点赞数 | 42 | |
| comment_count | 评论数 | 8 | |

### t_event_log（行为日志表）
| 字段 | 含义 | 示例 | 说明 |
|------|------|------|------|
| event_id | 事件 ID | 12345 | |
| user_id | 用户 ID | 1001 | 关联 t_user |
| event_type | 事件类型 | login/post_like/service_booking | 预定义枚举 |
| page | 页面 | home/community/ai_qa | |
| event_time | 事件时间 | 2025-03-20 14:30 | |
```

### 3.2 数据字典怎么来？

**不是手动写的——是 AI 自己分析的。**

我的做法：

1. 让 AI 连接数据库，`DESCRIBE` 每张表
2. 采样前 100 行数据，让 AI 推断字段含义
3. AI 生成初步字典，我 review 一遍
4. 业务口径补充（比如"user_id < 1000 是测试数据"这种规则）

### 3.3 为什么这个方式有效？

传统做法是找 DBA 或数据团队写文档——但问题是：

- 文档更新跟不上表结构变化
- 业务含义只有建表的人懂
- 新人入职看文档像看天书

用 AI 分析的方式：

- **实时更新**：表结构变了，AI 重新分析就行
- **业务语义分离**：AI 负责推断字段类型和关系，人负责补充业务口径
- **新人友好**：可以问"这个字段什么意思"，AI 用自然语言解释

---

## 四、SQL 规则：业务口径的沉淀

光有数据字典不够——**AI 写的 SQL 要符合业务口径。**

### 4.1 我的规则示例

```markdown
## 默认过滤规则
- 排除测试数据：user_id >= 1000
- 只算正式环境：env = 'prod'
- 时间范围：默认近 7 天，除非用户指定

## 指标定义（毛球业务口径）
- DAU：当天有登录行为的去重用户数（用 last_login_time 判断）
- 新增用户：created_at 在当天范围内的用户
- 内容发布率：DAU 中当天发布过内容的用户占比
- 服务预约转化率：浏览过附近服务页面的用户中完成预约的占比
- 留存：次日/7 日/30 日，用登录行为判断而非注册

## 输出格式
- Markdown 表格
- 按时间倒序
- 超过 100 行时只展示汇总
```

### 4.2 规则怎么来？

**跟 AI 对话对话出来的。**

最开始我没有规则，AI 生成的 SQL 经常不符合预期：

- 包含了测试数据 → 加规则 `user_id >= 1000`
- 时间范围不对 → 加规则"默认近 7 天"
- 输出太乱 → 加格式规范

每次发现问题，我就跟 AI 说"这样不对，应该是 XXX"，然后让它更新规则。

迭代了大概十几轮，现在稳定下来的规则覆盖了 90% 的场景。

---

## 五、当 AI 口径和业务口径不一致时

这是最大的坑。

### 5.1 问题

毛球的业务世界里有很多"中间表"——不是原始日志，是经过加工聚合的。

比如：
- 一张"每日活跃用户表"，已经聚合好了 DAU/MAU
- 一张"用户画像表"，打标了宠物品种偏好、消费等级等属性

这些表的口径是业务定义的，AI 直接看原始数据推导不出来。

### 5.2 解法：先解释逻辑，再执行

我现在的工作流：

1. 用户问毛球的复杂指标（比如"养猫用户的内容发布率"）
2. AI 先输出它对指标的理解："我理解养猫用户是 t_pet.species = 'cat' 的用户，发布率是当天有发帖行为的 DAU 占比，对吗？"
3. 我确认/纠正
4. AI 生成 SQL 执行

**多这一步，能避免 99% 的逻辑错误。**

而且这个对话过程本身也是知识沉淀——AI 记住这次校准，下次就懂了。

### 5.3 一个真实案例

毛球的业务世界里有很多"中间表"——不是原始日志，是经过加工聚合的。

比如一张"每日活跃用户表"，已经聚合好了 DAU/MAU；一张"用户画像表"，打标了宠物品种偏好、消费等级等属性。

这些表的口径是业务定义的，AI 直接看原始数据推导不出来。

有一次我问"复购率是多少"，AI 直接生成了 SQL。

结果跑出来的数据和我已知的对不上。

后来发现：我理解的"复购"是"同一用户在毛球商城下单≥2 次"，AI 理解的是"首次购买后再次购买的用户占比"。

两个定义都对，但算出来的数字差了一倍。

从那以后，我加了一条规则：**所有业务指标，第一次使用时必须先对齐定义。**

---

## 六、搞不定的场景

虽然我用了这么久，但 `/data` 不是万能的。

### 6.1 中间表没有权限

AI 只能访问它有权访问的表。毛球的核心推荐数据在数仓的 `dw_recommend_log` 表里，AI 只能连业务库的 `t_event_log`。那就跨不过去。

**变通**：我手动导出中间表，丢给 AI 分析。

### 6.2 自己都不了解表的时候

有时候业务表是别人建的、文档缺失、连字段名都是拼音缩写。

这时候 AI 也懵——它只能基于已有信息推断。

**变通**：先让 AI 采样数据、推断含义，再找建表的人确认。这个过程本身也是在帮你理解数据。

### 6.3 极度复杂的连表

超过 5 张表 JOIN、嵌套多层子查询、各种 CASE WHEN——这种 SQL AI 能写，但执行效率可能不如手写优化的。

**变通**：让 AI 写基础版本，我手动优化。

---

## 七、人人都是数据科学家？

这是我写这篇最想讨论的。

### 7.1 我的观察

"人人都是数据科学家"这个口号喊了很多年，但一直没实现。

原因是**工具门槛一直存在**——SQL 要学、数据模型要懂、可视化工具要熟练。

但 AI 把这个门槛抹平了。

现在的情况是：
- 你不用会写 SQL——用自然语言描述，AI 生成
- 你不用懂数据模型——数据字典 AI 维护
- 你不用学可视化——AI 直接输出图表

**唯一需要的能力是：知道要问什么问题。**

### 7.2 但这里有争议

有人会说："AI 生成的 SQL 你敢直接用吗？""口径不一致怎么办？""数据准确性谁负责？"

我的回答：

1. **不敢直接用→让 AI 先解释逻辑**（上面讲了）
2. **口径不一致→对话校准，AI 记住**（这也是知识沉淀）
3. **准确性→最终责任还是人**（AI 是工具，决策和验证还是你）

**"人人都是数据科学家"不是说不需要专家了——是说工具足够好用，让非专家也能做 80 分的数据分析。**

剩下的 20 分（复杂建模、数据治理、口径统一）还是需要专家，但日常分析需求 80% 都能自己搞定。

---

## 八、落地建议

如果你也想试试这个方案：

### 8.1 第一步：从一张表开始

不要一开始就想搞全量数据字典。

选你最常用的一张表（比如用户表、订单表），让 AI 分析结构、采样数据、生成初步字典。

### 8.2 第二步：定义 3-5 个核心指标

从你最常问的问题开始：
- DAU/MAU
- 新增用户
- 转化率
- 留存率

每个指标跟 AI 对齐定义，让它记住。

### 8.3 第三步：迭代规则

用几次后你会发现 AI 写的 SQL 有哪些问题。

每次发现问题，就加一条规则。迭代十几轮后，覆盖 90% 场景。

### 8.4 第四步：扩展数据源

一张表跑顺了，再慢慢加表。

优先加那些你经常 JOIN 的表（比如用户表→订单表→支付表）。

---

## 九、总结

这一年多的实践，我最大的收获不是"AI 能写 SQL"，而是：

**AI 可以成为你的数据知识库。**

它理解表结构、记得业务口径、知道哪些是测试数据、哪些指标怎么算。

你不需要记住所有表结构、所有字段含义、所有 SQL 语法。

你只需要知道**要问什么问题**。

---

## 十、讨论

我很好奇你在用什么方式做数据分析？

- 还是手动写 SQL 吗？
- 有试过让 AI 生成吗？效果怎么样？
- 你觉得"人人都是数据科学家"是机会还是坑？

欢迎在评论区聊聊。

---

**上一篇**：[[03-skills|第 03 篇：把重复工作变成一句话]]

**下一篇**：[[05-agent-team|第 05 篇：Agent Team]]

*作者：RicHe | 产品经理，专注 AI 产品设计*

---

**源文档**：[[content/sources/04-data-agent|GitHub 完整版]] · [[content/_index|系列总览]]