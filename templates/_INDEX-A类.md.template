# {目录名} — 目录索引

<!-- 替换 {目录名} 与 [占位] 内容。A 类目录 = 扁平目录：文件直接散在本目录下，没有项目子目录。 -->

> **类型**：A 类（扁平目录，文件直接在本目录下）
> **继承规则**：通用写文件三步见 `CLAUDE.md`，本目录特有规则在下方。

---

## 规则

> 只写本目录特有的约束，通用规则不重复。

- **命名**：`{主题}_{类型}_{YYYYMMDD}.md`（类型如：分析/方案/记录/草稿）
- **归档**：过期或被替代的文件移入 `_archive/`，不直接删除
- **大文件标记**：文件超过 30KB 时，在索引表文件名后标注 `[大]`，提示 AI 先读文件头（含章节速览）再定位目标章节，禁止全文读取
- **更新本索引**：新增文件后在下方「文件索引」追加一行，并在「变更日志」记录一条
- **变更日志封顶**：只保留最近 20 条，更早的合并/裁剪

---

## 文件索引

| 文件名 | 路径 | 一句话描述 | 关键词 | 更新日期 |
|--------|------|-----------|--------|----------|
| 示例文件 | `{目录}/{文件名}.md` | 文件的核心内容一句话 | 关键词1/关键词2 | YYYY-MM-DD |
| 示例大文件 `[大]` | `{目录}/{大文件名}.md` | 超过 30KB，先读文件头再定位 | 关键词 | YYYY-MM-DD |

<!-- 不用列所有文件，列重要的即可；每个文件一行：一句话描述 + 2-4 个关键词 + 更新日期 -->

---

## 变更日志

> 新增文件、重大修改、归档操作都记录在这里。保持最近 20 条。

| 日期 | 操作 | 说明 |
|------|------|------|
| YYYY-MM-DD | 新增 / 修改 / 归档 | 文件名 + 一句话说明变更内容 |
