# {目录名} — 目录索引

<!-- 替换 {目录名} 与 [占位] 内容。B 类目录 = 按项目分子目录的父目录。模板分两部分：
     上半部：目录层（父目录，只索引项目，不列文件）
     下半部：项目层（某个项目专属子目录的 _INDEX，含当前正本） -->

> **类型**：B 类（按项目分子目录）
> **继承规则**：通用写文件三步见 `CLAUDE.md`，本目录特有规则在下方。

---

## 规则（目录层）

- **文件必须放在对应项目子目录下**，不在本目录根部散放文件
- **命名**：项目子目录用 `{项目名}/`；子目录内的文件遵循各项目 `_INDEX.md` 的规则
- **查找指引**：先在下表找「项目」行，再读该项目的 `_INDEX.md`
- **更新本索引**：新增项目子目录后，在「项目目录」表追加一行

---

## 项目目录

| 项目 | 路径 | 状态 | 当前正本 | 备注 |
|------|------|------|----------|------|
| 示例项目 | `{目录}/{项目名}/` | 进行中 / 已归档 | `{项目名}/_CURRENT.md` 或主文档路径 | 一句话说明 |
| ... | ... | ... | ... | ... |

---

## 变更日志（目录层）

> 新增/归档项目、重大结构调整记录在这里。保持最近 20 条。

| 日期 | 操作 | 说明 |
|------|------|------|
| YYYY-MM-DD | 新增 / 归档 | 项目名 + 一句话说明 |

---

<!-- ================= 项目层模板（单个项目子目录内的 _INDEX.md 用以下结构）================= -->

# {项目名} — 项目索引

> **类型**：B 类项目层  ｜  **父目录规则**见 `{父目录}/_INDEX.md`

## 规则

- **当前依据文件**在下方「当前正本」表标注；旧版移入 `_archive/` 或标注「已被替代」
- **新建正本时**必须同步更新 `_CURRENT.md`（或「当前正本」表）和本索引的变更日志
- **大文件标记**：文件超过 30KB 在索引表标注 `[大]`，提示 AI 先读文件头再定位

## 当前正本

| 类型 | 当前正本路径 |
|------|-------------|
| 主文档 | `{目录}/{项目名}/{主文档}.md` |
| 规范 / 设计 | `{目录}/{项目名}/{规范文档}.md` |

## 文件索引

| 文件名 | 路径 | 一句话描述 | 关键词 | 更新日期 |
|--------|------|-----------|--------|----------|
| 示例文件 | `{目录}/{项目名}/{文件名}.md` | 核心内容一句话 | 关键词 | YYYY-MM-DD |

## 变更日志

> 新增文件、重大修改、归档操作记录在这里。保持最近 20 条。

| 日期 | 操作 | 说明 |
|------|------|------|
| YYYY-MM-DD | 新增 / 修改 / 归档 | 文件名 + 一句话说明 |
